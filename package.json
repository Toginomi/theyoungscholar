{
  "name": "theyoungscholar",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build --emptyOutDir",
    "lint": "eslint *.js src/**/*.js",
    "format": "prettier --ignore-path ./.gitignore --write \"./**/*.{html,json,js,ts,css}\"",
    "test": "jest",
    "dev": "npm run start"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
